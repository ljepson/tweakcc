// Please see the note about writing patches in ./index

import { showDiff } from "./index";

export const writeContextCollapse = (oldFile: string): string | null => {
  // TODO: implement context collapse patch
  return oldFile;
};
